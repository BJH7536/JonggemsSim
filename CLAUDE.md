# JonggemsSim — 종겜스 시뮬레이터

플레이어가 종합게임 스트리머가 되어 AI 시청자를 만족시키는 시뮬레이터.
AI 시청자는 플레이를 실시간 관측해 실제 인터넷 방송 채팅을 재현한다 (칭찬·야유·평가·훈수).

**출신**: NHN 해커톤 ai-spectator 아이디어션 트랙에서 분리해 새로 시작한 레포 (2026-08-06).
원 저장소는 `E:\NHN_Task` (우드 콘솔 카드 퍼즐 — 별개 프로젝트, 이 레포와 무관하게 보존).

## Key Documents (처음 읽는 순서)

1. `jonggems-concept.md` — **종겜스 기획서 원문 + 설계 리뷰.** 이 레포의 존재 이유
2. `prototypes/README.md` — 프로토타입 현황 (화력쇼 = 활성, 판돈 던전 = 보류)
3. `prototypes/05-hwaryeok-personas.md` — **AI 시청자 페르소나 8종 시트 + 시뮬레이터 확장 설계.**
   신규 시청자 설계는 반드시 이 문서의 3축 덮개·7축 모델 기준으로
4. `prototypes/05-hwaryeok-spec.md` — 화력쇼 스펙·밸런스 검증 이력 (critic 4차 승인 종결)
5. `10-ideas.md` / `00-brief.md` — 아이디어션 원천 (역사 자료)

## 공통 설계 규약 5종 (영구 준수)

1. **시청자 수는 체력바·점수·언락 통화를 겸한다** — 셋을 분리하면 도파민 루프가 흩어진다
2. **손실은 연출하지 않는다** — 숫자만 조용히. 획득은 과하게 연출
3. **큰 자극은 0.4초 간격 큐로만 방출** — 두 개의 폭발은 서로를 잡아먹는다
4. **신선도 감쇠 필수** — 100→70→45→25→10%, 미사용 구간 회복. 반복은 도파민의 적.
   종겜스에서는 **게임 단위 신선도**로 확장 (같은 게임만 파면 물린다 → 종겜이 메커닉이 된다)
5. **뼈대 vs 양념** — 실력 연동 스파이크가 뼈대, 랜덤(도네 등)은 양념. 양념을 뼈대로 쓰지 말 것

## AI 시청자 아키텍처 (화력쇼에서 검증 완료)

- **C3 원칙**: AI 시청자는 룰 수치에 일절 관여하지 않는다 — 관측하고 말만 한다
- **관측 = 이벤트 스트림**: 게임이 사실 슬롯을 발행하고 채팅이 반응. 화면 해석 방식 금지
- 톤 6종(hype/worry/info/mock/cheer/question) × 페르소나 = 톤 부분집합
- fact/flavor 분리: 버스트 첫 줄만 사실 슬롯, 나머지는 리액션
- 검증 게이트: 슬롯 포함 + 비반복(<0.72) + 재실패 시 폐기. LLM 교체 후에도 게이트는 밖에 유지
- LLM 교체 지점은 `Chat.compose()` 단일 — `[LLM-INTEGRATION-POINT]` 주석 필수
- 참조 구현: `prototypes/05-hwaryeok-show.html`의 `CHAT_*` 상수 + `Chat` 객체

## Technology Stack

- 프로토타입 단계: 단일 HTML / 바닐라 JS / 캔버스, 외부 의존성·네트워크 0건, `file://` 동작
- 로컬 QA: `python -m http.server 8770` (Chrome 확장이 file:// 차단 시)
- 이후 스택 결정은 최소 증명("게임 2개 + 공용 채팅") 이후에

## 커밋 정책

- **잘게, 과정이 보이게 커밋한다** — 해커톤 제출 요건(커밋 기록)이자 이 레포를 새로 시작한 이유
- `submission.local/` 은 어떤 경우에도 커밋 금지 (.gitignore 등재)
- 프로토타입 파일 수정 시 대응 스펙 문서(`05-*-spec.md` 류)의 검증 기록도 함께 갱신
