# 스프린트 — 공명 모델 v1 (8/7 → 8/10 제출)

> 결정: 정훈 ("사흘 안에 만든다", 2026-08-07). 스코프·규범: `ADR-002` + `resonance-model.md` §7.
> 원칙: **검증된 총량 위에 "구성 분포" 층만 얹는다** — 기존 밸런스 자산을 깨지 않는 최소 침습.

## 일정

| 날 | 목표 | 담당 |
|---|---|---|
| **D-0 (8/7)** | ADR-002·v1 사양 ✅ · STIM 초안 태깅 3게임 ✅ · 공명 샌드박스 v1 | 정훈 |
| **D-1 (8/8)** | 셸 판정층 구현(구성 상태·배분·f(V)·gain 확장) + UI(게이지·리포트·허브) + 캐스팅 반영 + 몬테카를로 5차 → PR | 정훈 구현 · **현재 리뷰**(contract 4.2 개정 포함) |
| **D-2 (8/9)** | 통합 튜닝(K·T_econ·언락) · Chrome 전 경로 검증 · 제출물 문서 반영 · 영상 촬영 | 전원 |
| **D-3 (8/10)** | Pages 활성화(오너) → 실접속 확인 → **제출** | 정훈 |

## 팀별 요청

- **현재**: ① PR #3 리뷰·머지 (스프린트 기반이 그 위에 쌓임) ② ADR-002 리뷰 — 특히 결정 2(해소 방향)와
  `gain(n, label, ev)` 확장(contract 4.2) ③ Vercel 키 + 프록시 재배포 (기존 건)
- **소윤**: ① 원형 흥미도 벡터·시작 구성 검토 (§7.2 — 샌드박스 뜨면 슬라이더로) ② STIM 태깅 초안 검토
  ③ 어휘·페르소나 작업은 부스로 계속 (스프린트와 병렬, 충돌 없음)
- **정훈(+에이전트)**: 위 일정의 구현 전부

## 리스크와 대응

- f(V)로 성장이 빨라져 언락 페이싱이 무너질 수 있다 → 몬테카를로 5차가 머지 게이트 (K·임계로 조정)
- D-1 구현이 밀리면 → **컷 순서**: 허브 "강한 관객" 표시 → 캐스팅 반영 → (최후) f(V)를 K=1로 봉인하고
  배분·게이지만 출시 (배분층은 총량 무변경이라 밸런스 무위험 — 최악에도 이건 나간다)
- 채팅·경제 동시 변경 충돌 → 스프린트 브랜치는 `feat/resonance` 단일, 소윤 어휘 작업은 data 계열만
