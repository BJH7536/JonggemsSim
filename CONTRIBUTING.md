# CONTRIBUTING — 협업 규칙

3인 팀 + 각자의 AI 에이전트. 분업 배경은 `docs/decisions/ADR-001-team-split.md`.

## 소유권 지도

| 경로 | 소유 | 내용 |
|---|---|---|
| `data/` | **소윤** (기획) | 페르소나 정의·이벤트 어휘·템플릿 — 코드 아님, 데이터 |
| `engine/` | **정훈** (관객) | 채팅 엔진·검증 게이트·데이터 로더·채팅 UI 컴포넌트 |
| `tools/` | **정훈** (관객) | 페르소나 부스, validate 등 기획 지원 도구 |
| `games/` | **현재** (무대) | 방송 셸·시청자 경제·개별 게임·배포 |
| `docs/contract.md` | **공동** | 이벤트 스키마·데이터 형식 — 변경 시 정훈·현재 상호 리뷰 필수 |
| `docs/decisions/` | 공동 | 미니 ADR — 결정은 여기 5줄로 남긴다 (무엇을·왜·대안) |
| `prototypes/` | 읽기 전용 | 검증된 원본 (화력쇼·판돈 던전) — 추출의 원천, 직접 수정하지 않는다 |

## 브랜치·PR 규칙 (딱 두 줄)

1. `engine/`·`games/`의 구조 변경과 `docs/contract.md` 변경 → **다른 엔지니어 리뷰 후 머지**
2. `data/` 변경 → **validate 통과 시 셀프 머지 가능** (`node tools/validate.mjs` —
   PR마다 GitHub Actions가 자동 실행)

브랜치는 짧게: `feat/…`, `game/…`, `data/persona-…`. 작업 단위마다 잘게 커밋한다 —
커밋 기록이 곧 해커톤 제출 증빙이다.

## AI 에이전트 컨벤션

- 에이전트 산출물도 전부 브랜치 + PR 경유. main 직push는 하지 않는다
- 에이전트 커밋은 `Co-Authored-By` 트레일러 유지
- 결정을 내렸으면 그 세션이 `docs/decisions/`에 ADR을 남긴다 — 다른 두 에이전트가
  재발명하지 않도록
- 각 영역 하위의 `CLAUDE.md`/`README.md`가 그 영역 에이전트의 출발 컨텍스트다 —
  구조가 바뀌면 그 문서부터 갱신할 것

## 첫 작업 (2026-08-06 기준)

- **정훈**: 화력쇼에서 채팅 엔진 추출 → `engine/` + `data/` 스키마 확정 → `contract.md` v1
  → 페르소나 부스. **소윤 병렬화의 선행 조건이라 최우선**
- **현재**: 게임↔셸 인터페이스 초안 → 방송 셸 v1 → 화력쇼 이식 → Pages 배포 뼈대
- **소윤**: `prototypes/05-hwaryeok-personas.md` 4장 기반 신규 캐스트·전용 어휘 설계.
  스키마 확정 전까지는 같은 문서의 시트 형식(마크다운)으로 작업
